export class Course {
  id: number;
  name: string;
  description: string;
  code: string;
  addedDate: Date;
  active: number;
  constructor(/*id: number, name: string, description: string,
              code: string, addedDate: Date, active: number*/) {
    /*this.id = id;
    this.name = name;
    this.description = description;
    this.code = code;
    this.addedDate = addedDate;
    this.active = active;*/
  }
}
